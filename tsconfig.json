{
    "compilerOptions": {
        "outDir" : "dist",
        "target": "ES2020",  // ciblé un navigateur plus récent
        "noEmitOnError" : true,  // ne pas compiler en cas d'erreur
        "strict": true, // Permet de passer le mode en strict
    },

    "files" : [
        "src/app.ts"
    ]
}


// npm init -y
// npm install
// nps install typescript --save-dev
//  npx tsc -- watch permet de lancer la compilation automatique